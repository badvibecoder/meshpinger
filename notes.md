
Currently we have to use --break-system-packages to force pyyaml on remote nodes

For this and future additional tests we will want to move to a venv when running remotely. 

ansible can handle execution in remote venvs 
